#include <stdio.h>
#include <math.h>
#include <string.h>

int main()
{

char seq[ 5000 ];
int  i, len;
for ( i=0 ; i<10 ; i++)
	{

		scanf( "%s", seq );
		len = strlen( seq );
		printf( "The length is %d\n", len );

	}

}
